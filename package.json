{
  "dependencies": {
    "axios": "^1.3.4",
    "puppeteer": "^19.8.0",
  },
  "scripts": {
    "dev": "nodemon",
    "build": "rimraf .build && tsc",
    "start": "npm-run build && node build/index.js"
  },
  "devDependencies": {
    "@types/node": "^18.15.10",
    "nodemon": "^2.0.22",
    "rimraf": "^4.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
